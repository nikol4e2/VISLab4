# Даден примерок
sample <- c(4, 5, 6, 2, 3, 4, 1, 4, 7, 5)

# Геометриска распределба: функцијата на веројатност
# P(X = x) = (1-p)^(x-1) * p

# МПО процена за параметарот p е дадена како:
# p = 1 / mean(sample)

# Проценка на p
p_hat <- 1 / mean(sample)
p_hat
